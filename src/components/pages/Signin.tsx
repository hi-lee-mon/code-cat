import { Typography } from "@mui/material"
import { Link } from "react-router-dom"

export const Signin = () => {
  return (
    <div>
      <Typography>製造中</Typography>
      <Link to="/login">ログイン画面へ</Link>
    </div>
  )
}
